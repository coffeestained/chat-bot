name: Needs-Rework

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  print-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a message
        run: echo "Temporary workflow until we have a proper deployment workflow"